# SQL Basics

## What I Learned Today
- Creating tables with CREATE TABLE
- Inserting data with INSERT INTO
- Selecting data with SELECT
- Filtering with WHERE
- Sorting with ORDER BY
- Limiting results with LIMIT
- Counting with COUNT and SUM
- Grouping with GROUP BY
- Column aliases with AS

## SQL Keywords I Know
SELECT, FROM, WHERE, ORDER BY, LIMIT, COUNT, SUM, GROUP BY, AS, DESC, ASC

## Queries I Wrote
- Selected specific columns from a table
- Filtered products by category and price
- Sorted results by price (highest first)
- Counted total orders
- Grouped products by category
- Found total quantity sold per product
